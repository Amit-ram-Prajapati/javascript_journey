let num = 20 , num1 = 30;
let temp = 0;
console.log(`Before swapping numbers are ${num} ${num1}` )
temp = num ; // 20
num = num1; // 30
num1 = temp ; // 20
console.log(`After swapping numbers are ${num} ${num1}` )
